# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SkyFall Alert' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'SnapKit', '~> 5.6.0'
  pod "PromiseKit", "~> 6.8"
  # Pods for SkyFall Alert

  target 'SkyFall AlertTests' do
    inherit! :search_paths
  end

  target 'SkyFall AlertUITests' do
  end

end
